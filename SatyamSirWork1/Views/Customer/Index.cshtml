@model SatyamSirWork1.Models.tbl_customer
@{
    /**/

    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@using (Html.BeginForm("Index", "Customer", FormMethod.Post, new { id = "Customerform" }))
{
    @Html.AntiForgeryToken();
    HtmlHelper.UnobtrusiveJavaScriptEnabled = false;



    <h3>Add Customer</h3>
    <div class="form-horizontal">
        

        
        <div class="form-group">
            <div class="row">
                <label for="Customer Name" class="col-md-3 control-label">Customer Name</label>
                <div class="col-lg-9 col-md-9">
                    @Html.TextBoxFor(m => m.cust_name, new { @class = "form-control", placeholder = "Customer Name" })
                    @*@Html.ValidationMessageFor(m => m.cust_name, "", new { @class = "text-danger" })*@
                </div>
            </div>
        </div>


        <div class="form-group">
            <div class="row">
                <div id="errmsg" style="color:red;"></div>
                @Html.LabelFor(m => m.cust_age, new { @class = "col-md-3 control-label" })
                <div class="col-lg-9 col-md-9">
                    @Html.TextBoxFor(x => x.cust_age, new { @class = "form-control", placeholder = "Age" })
                    @*@Html.ValidationMessageFor(m => m.cust_age, "", new { @class = "text-danger" })*@

                </div>
            </div>
        </div>
        <div class="form-group">
            <div class="row">
                <div id="errmsg1" style="color:red;"></div>
                @Html.LabelFor(m => m.cust_contact, new { @class = "col-md-3 control-label" })
                <div class="col-lg-9 col-md-9">

                    @Html.TextBoxFor(x => x.cust_contact, new { @class = "form-control", placeholder = "Contact" })
                    @*@Html.ValidationMessageFor(m => m.cust_contact, "", new { @class = "text-danger" })*@
                </div>

            </div>
        </div>
        <div class="form-group">
            <div class="row">

                @Html.LabelFor(m => m.cust_email, new { @class = "col-md-3 control-label" })
                <div class="col-lg-9 col-md-9">
                    @Html.TextBoxFor(x => x.cust_email, new { @class = "form-control", placeholder = "EmailId" })
                    @*@Html.ValidationMessageFor(m => m.cust_email, "", new { @class = "text-danger" })*@

                </div>

            </div>
        </div>
        <div class="form-group">
            <div class="row">

                @Html.LabelFor(m => m.cust_address, new { @class = "col-md-3 control-label" })
                <div class="col-lg-9 col-md-9">
                    @Html.TextAreaFor(x => x.cust_address, new { @class = "form-control", placeholder = "Address" })
                    @*@Html.ValidationMessageFor(m => m.cust_address, "", new { @class = "text-danger" })*@
                </div>
            </div>
        </div>
        <div class="form-group">
            <div class="row">
                <div id="errmsg2" style="color:red;"></div>
                @Html.LabelFor(m => m.cust_pincode, new { @class = "col-md-3 control-label" })
                <div class="col-lg-9 col-md-9">
                    @Html.TextBoxFor(x => x.cust_pincode, new { @class = "form-control", placeholder = "PinCode" })
                    @*@Html.ValidationMessageFor(m => m.cust_pincode, "", new { @class = "text-danger" })*@

                </div>
            </div>
        </div>
        <div class="form-group">
            <div class="row">

                <div class="col-md-offset-3 col-md-9">
                    <input type="submit" value="Save" class="btn btn-success" onclick="validateme();" />
                </div>
            </div>
        </div>

    </div>

}
<script src="@Url.Content("~/Scripts/jquery-3.3.1.min.js")" )></script>
<script src="@Url.Content("~/Scripts/jquery.validate.min.js")"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")"></script>

<script type="text/javascript" language="javascript">


    $("#Customerform").validate({
        rules: {
            cust_name: "required",
            cust_age: "required",
            cust_email: "required",
            cust_address: "required",
            cust_pincode: "required",
            cust_contact: "required"

        },
        messages: {
            cust_name:
                { required: "Please enter customer name" },
            cust_age: "Please enter age",
            cust_email: "Please enter email",
            cust_address: "Please enter address",
            cust_pincode: "Please enter pincode",
            cust_contact: "Please enter contact"
        },
    }
    );


    function validateme() {
        //var email = $("#cust_email").val();
        //var emailreg = new regexp(/^(("[\w-\s]+")|([\w-]+(?:\.[\w-]+)*)|("[\w-\s]+")([\w-]+(?:\.[\w-]+)*))(@@((?:[\w-]+\.)*\w[\w-]{0,66})\.([a-z]{2,6}(?:\.[a-z]{2})?)$)|(@@\[?((25[0-5]\.|2[0-4][0-9]\.|1[0-9]{2}\.|[0-9]{1,2}\.))((25[0-5]|2[0-4][0-9]|1[0-9]{2}|[0-9]{1,2})\.){2}(25[0-5]|2[0-4][0-9]|1[0-9]{2}|[0-9]{1,2})\]?$)/i);
        //var valid = emailreg.test(email);
        //if (email == "") {
        //    $("#div1").text("please enter an email");
        //}
        //else if (!valid) {
        //    $("#div1").text(" email is not valid");
        //}

        var Phonenumber = $("#cust_contact").val();
        var filter = /^[0-9-+]+$/;
        if (!(filter.test(Phonenumber))) {

        }
    }
</script>
<script>
    $(document).ready(function () {

        $('#cust_contact').keypress(function (e) {
            if (e.which != 8 && e.which != 0 && (e.which < 48 || e.which > 57)) {

                $("#errmsg").html("Digits Only").show().fadeOut("slow");
                return false;
            }
        });
        $('#cust_pincode').keypress(function (e) {
            if (e.which != 8 && e.which != 0 && (e.which < 48 || e.which > 57)) {

                $("#errmsg1").html("Digits Only").show().fadeOut("slow");
                return false;
            }
        });
        $('#cust_age').keypress(function (e) {
            if (e.which != 8 && e.which != 0 && (e.which < 48 || e.which > 57)) {

                $("#errmsg2").html("Digits Only").show().fadeOut("slow");
                return false;
            }
        });
    });
</script>